# Implementation Plan

- [ ] 1. 基盤: 解放処理を常駐リーパーへ移す

- [x] 1.1 常駐リーパーを実装する
  - プロセスにつき 1 つだけ生成され、プロセス終了まで生存する実行主体を用意する
  - 解放要求（プールのクローンとスキーマ名）を受け取り、プールを閉じてからスキーマを削除する
  - 送信操作は呼び出し側をブロックせず、パニックもしない。`Drop` の内側から安全に呼べる
  - 解放の成否でテストを失敗させない。失敗はログに留める
  - 完了状態: 解放要求を送ったあと呼び出し側のランタイムが破棄されても、プールが閉じられ
    スキーマが削除されることを検証する単体テストが緑になる
  - _Requirements: 2.1, 2.3_
  - _Boundary: HarnessReaper_

- [x] 1.2 TestApp の解放をリーパー委譲に置き換える
  - `Drop` の実装を、スキーマ削除のデタッチからリーパーへの送信に置き換える
  - 明示的な後始末の経路は変更しない（既存の呼び出し側を書き換えない）
  - 同一プールに対して明示経路とリーパー経路の両方が走っても破綻しないことを確認する
  - 完了状態: 後始末を呼ばずに多数のインスタンスを生成・破棄しても、保持接続数が
    生存インスタンス数に比例する範囲に留まることを検証する統合テストが緑になる
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_
  - _Depends: 1.1_

- [ ] 2. 孤立スキーマの回収

- [x] 2.1 (P) 回収可否の判定を実装する
  - スキーマ名に埋まった生成時刻をパースし、閾値より古いものだけを回収対象と判定する
  - パースできない名前、閾値より新しい名前は対象外とする（実行中の他プロセスを巻き込まない）
  - 完了状態: 古い名前・新しい名前・パース不能な名前それぞれに対する判定を検証する
    単体テストが緑になる
  - _Requirements: 3.2_
  - _Boundary: OrphanSchemaSweeper_

- [x] 2.2 起動時スイープを実装し初回フィクスチャ生成に接続する
  - プロセス内で最初にフィクスチャが生成されたときに 1 度だけ実行する
  - 回収失敗はログに留め、テスト実行を止めない
  - 完了状態: 古い孤立スキーマを人工的に作った状態でテストを実行すると回収され、
    新しいスキーマは残ることを検証する統合テストが緑になる。事前のスキーマ掃除なしで
    テストが実行できる
  - _Requirements: 1.2, 3.1, 3.3, 3.4_
  - _Depends: 2.1_

- [ ] 3. 軽量 DB フィクスチャへの移行

- [x] 3.1 軽量フィクスチャを実装する
  - 隔離スキーマとマイグレーション適用済みプールのみを提供する
  - ルーター構築・待受開始・サーバータスク生成・モジュール構築・署名鍵生成を行わない
  - 決定性の注入（clock / id / RNG）は既存の実インスタンス用フィクスチャと同一の規約に従う
  - 解放は 1.1 のリーパーに委譲する
  - 完了状態: 実インスタンスを起動せずにマイグレーション適用済みのプールが得られることを
    検証する単体テストが緑になる
  - _Requirements: 4.2_
  - _Depends: 1.1_
  - _Boundary: TestDb_

- [x] 3.2 移行対象を分類する
  - 実インスタンスを使っているテストと、SQL とドメイン型だけで完結するテストを判別する
  - 判別基準は design の使い分け基準（HTTP 送出・ルーター・トークン・モジュール参照の
    いずれかを使うか）に従う
  - 完了状態: モジュールごとの移行対象件数と非対象件数が一覧として得られ、合計が現状の
    実インスタンス起動件数と一致する
  - _Requirements: 4.1_
  - _Depends: 3.1_

- [x] 3.3 (P) 投稿系モジュールのテストを移行する
  - 分類結果に従い、対象テストを軽量フィクスチャへ移す
  - 完了状態: 当該モジュールのテスト件数と成否が移行前と一致し、実インスタンス起動件数が
    分類結果のぶん減っている
  - _Requirements: 4.1, 4.4_
  - _Depends: 3.2_
  - _Boundary: statuses, timelines_

- [x] 3.4 (P) ソーシャルグラフ・通知・検索モジュールのテストを移行する
  - 完了状態: 当該モジュールのテスト件数と成否が移行前と一致し、実インスタンス起動件数が
    分類結果のぶん減っている
  - _Requirements: 4.1, 4.4_
  - _Depends: 3.2_
  - _Boundary: social_graph, notifications, search_

- [x] 3.5 (P) 認証・アカウント・メディア・連合モジュールのテストを移行する
  - 完了状態: 当該モジュールのテスト件数と成否が移行前と一致し、実インスタンス起動件数が
    分類結果のぶん減っている
  - _Requirements: 4.1, 4.4_
  - _Depends: 3.2_
  - _Boundary: oauth, accounts, actor, media, federation_

- [ ] 3.6 移行後の配置規約を確認する
  - 実起動インスタンスを要する検証が単体テスト側に残っていないか確認し、残っていれば
    統合テストの配置規約に従って移す
  - 完了状態: 実インスタンス起動を要する検証がすべて統合テストの配置規約に沿って置かれている
  - _Requirements: 4.5_
  - _Depends: 3.3, 3.4, 3.5_

- [ ] 4. テストハーネスの本番成果物からの分離

- [ ] 4.1 テスト構成のビルド設定を導入する
  - ハーネスを有効化するビルド構成を定義し、テストビルドから利用できるようにする
  - この段階ではまだハーネスの公開をゲートしない（統合テストを壊さない順序を守る）
  - 完了状態: テスト構成が定義され、既存のテストがすべて従来どおりビルド・実行できる
  - _Requirements: 5.3_

- [ ] 4.2 ハーネスの公開をビルド構成でゲートする
  - テスト構成が無効なときハーネスとその固定値（固定鍵・固定パスフレーズ・テスト用接続先）を
    成果物に含めない
  - 連合ペアハーネスの公開も同様にゲートする
  - 完了状態: テスト構成を無効にしてビルドした成果物にテスト専用の固定値が含まれず、
    本番依存グラフからハーネス由来の依存エッジが消えている
  - _Requirements: 5.1, 5.2, 5.4_
  - _Depends: 4.1_

- [ ] 5. 配送ワーカーのクレーム失敗の再現条件を特定し結論を記録する
  - 接続プールを 1 に縮小した際にクレーム件数が 0 になる現象の再現条件を特定する
  - 第一候補（実行中に同じプールの別経路が対象行をロックしている）から検証する
  - プロダクションの欠陥か、テスト固有の事情かを判定し、根拠とともに記録する
  - 判明しない場合も、否定された仮説を記録して閉じる（原因不明のまま「flake」として放置しない）
  - 結論に応じて steering の既知の問題の記述を実態へ更新する
  - 完了状態: 現象について「何が原因か」または「何が否定されたか」が根拠付きで記録され、
    steering の記述が実態と一致している
  - _Requirements: 6.1, 6.2, 6.3, 6.4_

- [ ] 6. 統合検証

- [ ] 6.1 単一プロセスでの一括実行を確認する
  - 事前のスキーマ掃除もモジュール分割もせずに全単体テストを単一プロセスで実行する
  - 失敗が出た場合、資源枯渇に起因するものが 1 件も含まれないことを確認する
  - 完了状態: 一括実行が資源枯渇由来の失敗ゼロで完了し、モジュール単位実行と成否が一致する
  - _Requirements: 1.1, 1.3, 1.4_
  - _Depends: 1.2, 2.2, 3.6, 4.2_

- [ ] 6.2 実行時間と残存スキーマを測定する
  - 一括実行の所要時間を測定し、移行前のベースライン（891 秒）と比較する
  - 実行後に残存する孤立スキーマ数を測定し、移行前のベースライン（239 個）と比較する
  - 実インスタンス起動を要するテスト件数を測定し、移行前のベースライン（750 件）と比較する
  - 完了状態: 3 指標の測定値がベースラインとともに記録され、実行時間が短縮されている
  - _Requirements: 4.3_
  - _Depends: 6.1_

## Implementation Notes

- 1.1: `src/test_harness.rs` の `mod reaper;` に付けた `#[allow(dead_code)]` は 1.1/1.2 分割による
  過渡的なもの。`src/lib.rs` が `pub mod test_harness` を無ゲートで公開しているため、production
  caller（`Drop for TestApp`）が来る 1.2 まで全項目が未使用警告になる。**1.2 でこの属性を外すこと。**
- **3.3 の重要な発見（要件 4.3 の前提が経験的に否定された）**: `TestDb` 移行では実行時間が
  短縮されない。`statuses` 372 件はベースライン 119 秒 → 移行後 115 秒（レンジが重なる）、
  `timelines` は横ばい。完全移行済みモジュール（`tag_repository` 13/13）で 10 回ずつ交互測定
  しても**下限が同一**（約 5.03 秒）。移行は効いている（`spawn_test_db` はルーター・待受・
  モジュール構築・鍵生成を実際に飛ばす）が、そこが時間を食っていなかった。支配的なのは
  両フィクスチャが共有する `establish_isolated_db`（スキーマ作成 + プール確立 + 11 本の
  マイグレーション適用）で、フィクスチャ 1 個ごとに走る。
  → **要件 4.1（件数削減）は満たされるが、要件 4.3（実行時間短縮）は 3.3–3.5 では前進しない。**
  design の「4.3 は TestDb 移行の副次的効果として測定する」という前提は誤り。4.3 のレバーは
  フィクスチャあたりのマイグレーション費用（テンプレートスキーマ複製など）であり、design の
  Non-Goals はそれを明示的に対象外にしている。**タスク 6.2 の前にこの矛盾の扱いを決める必要がある。**
  ただし 891 秒のベースラインには `PoolTimedOut` で 5 秒待って落ちる 256 件が含まれており、
  リーク解消だけで一括実行時間が下がる可能性は別途ある（6.1 / 6.2 で実測して判断する）。
- 3.5: `runtime.keys` の静かな乖離経路は**閉じたことを独立に確認済み**。`ProcessingWorker`
  （`src/media/worker.rs`）は `runtime` を保持するが読むのは `clock` のみ、`OauthService`
  （`src/oauth/service.rs`）も `clock`/`ids`/`rng` しか触らない。`FixedSigningKeyProvider` への
  差し替えは両経路で実際に無害。実鍵に依存する 3 ファイルは据え置きのまま無変更。
- 3.5: バックグラウンドループ不在は、キュー系テストを**空虚にするのではなく強くしていた**。
  `claim_due_does_not_return_a_job_whose_next_attempt_at_is_in_the_future` は `TestApp` では
  実配送ポーラーが行を奪って「別の理由で空」になり得たが、`TestDb` では `status == Pending` の
  再読み込みと合わせて「誰も奪っていない」ことを証明する。lease 再取得テストも同様。
- 3.5 の残タスク候補（本 spec の範囲外）: `src/media/job_queue/tests.rs:178` のコメントが
  `max_connections = 5` と書いているが実値は 2（`2703704` で 5→2 にした際に取り残された）。
  テストの前提自体は成立しているので害はない。
- **3.4 で見つかった既存のリーク（6.1 の入力）**: `social_graph` のテストを 1 回走らせるごとに
  ハーネススキーマが 1 個残留する（サフィックス `_131` で安定＝プロセス内 `AtomicU64` の位置が
  決定的）。移行前のツリーでも同一に再現するので**本 spec の変更が原因ではない**。`notifications`
  と `search` の残留は 0。所有テストは未特定（`br#"{…}"#` の生文字列があってブレース対応での
  自動特定が効かない）。要件 2.4 / 2.5 とタスク 6.1 に関わる。
- 3.4: 実行時間は 3 モジュールとも低下した（レビュアーの同一セッション内実測: 59.7→51.7 /
  35.9→31.6 / 35.0→29.5 秒）。ただし実装者の測定では逆に微増しており、**測定間のばらつきが
  効果より大きい**。単発の測定で 4.3 を判断しないこと。
- 3.4: `cargo test --lib notifications` は 124 件、`cargo test --lib -- notifications::` は
  117 件を報告する（前者は部分文字列一致で他モジュールのテストも拾う）。6.2 で件数を記録する
  ときはフィルタの形を揃えること。
- 3.3: 移行後に `tag_repository` の裾が重くなった（10 回中 4 回が 8 秒超、ベースラインは 0 回。
  下限は不変）。原因未特定、全実行が緑。6.2 で確認する価値がある。
- 3.2: 分類結果は `.kiro/specs/test-infrastructure/migration-classification.md`。745 件中
  **539 件が移行対象**（機械判定 540 から、ハーネス自己検証テスト 1 件を除外）。3.3–3.5 は
  この文書だけで自グループの対象を一意に特定できる。混在ファイルは 3 つだけで、残りは
  ファイル単位で all-or-nothing。
- 3.2: design の「750 件」はテスト件数ではなく `spawn_test_app(` の**テキスト出現回数**だった。
  実件数は `#[tokio::test]` 関数で 745。タスク 6.2 でベースラインと比較する際はこの違いに注意。
- 3.2: 両フィクスチャでコンパイルが通るのに挙動が変わる経路は 2 つ。(a) `runtime.keys`
  （`TestApp` は実 DB 由来、`TestDb` は固定値）、(b) `spawn_test_app` が起動する配送・プルーニング・
  メディアの各バックグラウンドループが `TestDb` には無い。(b) は決定性が上がる方向だが、
  移行後にキュー系テストの挙動が変わったらまずこれを疑うこと。
- 3.1: `TestDb.runtime.keys` は `RuntimeContext::deterministic` の `FixedSigningKeyProvider` の
  まま。design の TestDb Invariant が clock / id / RNG のみを列挙し keys を意図的に外していること、
  および「署名鍵生成を行わない」が根拠。`DbSigningKeyProvider` は `KeyCache` と `ActorModule` を
  要求するので、そもそも TestDb が禁じられているモジュール構築が必要になる。
- 3.1: schema+pool+migrate の並びは `establish_isolated_db()` に集約済み。`spawn_test_app` と
  `spawn_test_db` の両方がこれを通る。新しいフィクスチャを足すときもここを通すこと。
- **未解決の欠陥（要 follow-up、本 spec の要件 3.1 / 1.2 に関わる）**: `src/federation/test_harness.rs`
  の連合ペアハーネスは schema+pool+migrate の 3 つ目の複製を持ち、(a) 起動時スイープを呼ばず、
  (b) スキーマ接頭辞が `kawasemi_federation_pair_` で `HARNESS_SCHEMA_PREFIX` に一致しない。
  結果として**異常終了で孤立した連合ペアのスキーマはどのスイープでも回収できない**。正常終了時は
  `TestApp` の `Drop` がリーパーへ委譲するので実害は出ていない（実測でも残存 0）。タスク 6.1 の
  一括実行前に判断が要る。
- 2.2: 回収閾値は 2 時間（フルスイート 891 秒の約 8 倍）。誤射しても被害は「そのプロセスの以後の
  クエリが `relation does not exist` で大きな音を立てて落ちる」であって静かな破壊ではないが、
  過剰回収は不可逆なので長めに倒してある。
- 2.2: スキーマ一覧に `LIKE 'kawasemi_test_harness_%'` を使わないこと。**`_` は LIKE の
  ワイルドカード**なので、このパターンは見た目より広く一致する。全件列挙して Rust 側の
  `is_reclaimable` で絞る。
- 2.2 申し送り（4.2 の完了条件に含めること）: `pub mod sweep` と `sweep_orphans_now` /
  `startup_sweeps_performed` は統合テストから駆動するために design のインターフェースを超えて
  公開している。`test-harness` feature ゲートの内側に確実に入れること（Requirement 5.2）。
- 2.1: 回収述語の失敗様式は非対称。過剰保持は安いが過剰回収は実行中の別プロセスの DB 状態を
  破壊する。パース不能・未来時刻・桁溢れはすべて「保持」に倒す。特に `duration_since` を
  `Result` で受けること（`Duration` の減算 underflow は「巨大な age＝回収対象」に化ける）。
- 2.1: `unique_schema_name` は `sweep::HARNESS_SCHEMA_PREFIX` を参照する形にした（命名規約と
  判定側の結合を構造化）。`tests/harness_release_it.rs` は統合バイナリで `pub(crate)` が
  見えないため独自の複製を持つが、同ファイルが実 DB の `current_schema()` に対して
  `starts_with(HARNESS_SCHEMA_PREFIX)` を主張しているのでドリフトは検知される。
- 2.2 でやること: `sweep.rs` の `is_reclaimable` に付いた `#[allow(dead_code)]` を外し、
  閾値の実値を決める（フルスイート約 891 秒より十分に大きく取る）。
- 1.2: 真因の内訳が判明した。統合テストのプロセスでは外側ランタイムが生き残るため、旧 `Drop` の
  デタッチ経路でも**スキーマ自体は落ちていた**。一度も起きていなかったのは `pool.close()` で、
  破棄ごとに約 2 接続ずつ積み上がっていた（40 個中 10 個目で baseline+22）。
- 1.2: 共有テスト DB を測るテストは**テスト間分離を自前で持つこと**。`information_schema` を
  サーバ全体で舐めると兄弟テストの生存スキーマを自分の残骸と誤検出して flaky になる（実際に
  1 回目 FAIL / 2 回目 PASS を踏んだ）。自プールへの `current_schema()` で自分のスキーマを特定し、
  `schema_name = ANY($1)` で自分のぶんだけを見る。加えてバイナリ内は静的 `tokio::sync::Mutex`
  （poison を持たないのでパニックするテストが波及しない）で直列化する。
- 1.2 申し送り: `pg_stat_activity` による接続数の観測は「同時に別のテストバイナリが同じ DB を
  触らない」ことに依存する。`cargo test` はテストターゲットを逐次実行するので現状は成立するが、
  `cargo nextest` 等のプロセス並列実行器を導入すると崩れる。タスク 6.x で実行形態を変える際は
  この前提を確認すること。
- 1.1: リーパーは 1 スレッド + current-thread ランタイムで逐次処理し、`drop_schema` ごとに
  短命 admin プールを開閉する（同時 admin 接続は最大 1）。fan-out させると本 spec が解消しようと
  している接続枯渇を悪化させるため、この逐次性は意図的。
