# Implementation Plan

段階の順序は design.md「Migration Strategy」に従う。**各段階（メジャータスク）の完了時点でフルスイートがグリーンであること**が共通の完了条件（Requirement 1.6）。テスト期待値を変更した場合は、それが仕様変更ではなく「テスト側が誤っていた」ことの修正である根拠を実装記録に残す（Requirement 1.4）。

---

- [x] 1. 失敗理由の識別をエラーメッセージ文字列から型へ移す

- [x] 1.1 横断エラー型にドメイン識別子を導入する
  - 横断エラー型に「どのドメイン条件で失敗したか」を表す省略可能な識別子を追加する。バリアントは重複投票の 1 種類のみとし、将来のための先回りバリアントは作らない
  - 識別子を設定できる 4xx 用コンストラクタを追加する。既存の 4xx / 5xx コンストラクタはシグネチャを変えず、識別子は未設定で埋める
  - 識別子はレスポンスボディにもログにも一切出さない。既定のボディレンダラーと Mastodon 互換ボディレンダラーのいずれも識別子を読まない
  - 完了状態：識別子を設定したエラーと設定していないエラーが、同一のステータスコード・同一のレスポンスボディを返すことを単体テストが示す
  - _Requirements: 1.1, 2.3, 2.4, 2.6_
  - _Boundary: AppError_

- [x] 1.2 重複投票の判定を型に切り替える
  - 投票記録における「既に投票済み」の拒否にのみ識別子を付ける。期限切れ・選択肢範囲外・単一/複数選択違反の 3 つの拒否は識別子を付けない
  - 拒否時のメッセージ文言とステータスコードは現状のまま一字も変えない
  - 受信 Activity ハンドラの重複投票分岐を、メッセージ文字列の比較から識別子の比較に置き換える。ステータスコードの比較は冗長になるため削除する
  - 完了状態：投票記録が返す 4 種類の拒否のうち重複投票だけが識別子を持つことを単体テストが示し、受信ハンドラが重複投票のみをべき等（処理済み）として扱い他の 3 つはエラーとして伝播することを既存テストが示す
  - _Requirements: 1.6, 2.1, 2.2, 2.5, 2.6_
  - _Boundary: PollRepository, InboundActivityHandler_

---

- [x] 2. エンドポイント横断の定型処理を横断層に集約する

- [x] 2.1 (P) クエリパラメータ解釈の単一実装を用意する
  - 件数上限パラメータの解釈（省略時・有効な 10 進数・非数値）と、緩い真偽値パラメータの解釈（真/偽の複数表記と、それ以外の拒否）を横断層に置く
  - 既存の private 実装からバイト単位で挙動を写す。「改善」しない
  - 移設元に紐づいていた単体テストを横断層側へ移す
  - 完了状態：横断層の単体テストが、省略時・有効値・非数値・真偽値の各表記について移設前と同一のステータスコードとメッセージを返すことを示す
  - _Requirements: 3.1, 3.4_
  - _Boundary: api::query_

- [x] 2.2 (P) 時刻整形の単一実装を用意する
  - エンティティの時刻フィールドを出力するための RFC3339 整形を横断層に置く
  - 4 箇所の private 実装が同一であることを確認した上で写す
  - 完了状態：横断層の単体テストが、移設前の各実装と同一の文字列を返すことを示す
  - _Requirements: 3.3_
  - _Boundary: api::time_

- [x] 2.3 (P) データストア障害のエラー変換の単一実装を用意する
  - データストアのエラーを横断エラー型のサーバーエラーへ変換する処理を横断層に置く
  - 同名の 11 実装が同一であることを確認して写す。名前の異なる残り 15 実装は、返すエラーの形（ステータス・公開メッセージ）が同一であることを個別に確認し、**同一と確認できたものだけ**を対象にする。異なるものは移設せず、その旨を実装記録に残す
  - 完了状態：横断層の単体テストが、変換後のステータスコードと公開メッセージが移設前と一致することを示し、移設対象外とした実装の一覧とその理由が記録されている
  - _Requirements: 3.5_
  - _Boundary: api::db_

- [x] 2.4 (P) 自インスタンスのオリジン解決の単一実装を用意する
  - リクエスト文脈を持たない場所（Activity 構築、通知やハッシュタグのシリアライズ）から自インスタンスの正規オリジンを組み立てる処理を横断層に置く
  - 完了状態：横断層の単体テストが、移設前の呼び出しと同一のスキームとホストを持つオリジンを返すことを示す
  - _Requirements: 3.6_
  - _Boundary: api::origin_

- [x] 2.5 エンドポイントとシリアライザを横断層の実装に移行する
  - 件数上限・真偽値・時刻整形の private 実装をすべて削除し、横断層の実装を呼ぶ。対象はアカウント／ソーシャルグラフ／タイムライン／通知／投稿／検索の各エンドポイントと、アカウント／投稿／通知の各シリアライザ
  - 緩い真偽値の解釈は分析レポートが 2 箇所としているが実測は 3 箇所（検索が数え落とされている）。3 箇所すべてを対象にする
  - 移設元の private 実装に紐づく既存テストは削除せず、横断層側へ移すか呼び出し経由で残す
  - ページネーション可能な全 7 箇所が横断層のリンクヘッダー組み立てを経由していること（独自組み立てが残っていないこと）を確認する。既に一元化されている想定であり、逸脱が見つかった場合のみ修正する
  - 完了状態：対象モジュールに件数上限・真偽値・時刻整形の private 実装が 1 つも残っておらず、リンクヘッダーの独自組み立ても存在せず、各エンドポイントの既存テストがグリーン
  - _Requirements: 1.1, 1.6, 3.1, 3.2, 3.3, 3.4, 3.7_
  - _Depends: 2.1, 2.2_

- [x] 2.6 エラー変換とオリジン解決を横断層の実装に移行する
  - 2.3 で同一と確認したデータストアエラー変換の private 実装を削除し、横断層の実装を呼ぶ
  - 自オリジン解決の 5 箇所（通知サービス／検索ハイドレータ／タグシリアライザ／アカウント投稿プロバイダ／フォローリクエストサービス）を横断層の実装に置き換える
  - 完了状態：移設対象としたエラー変換の private 実装が残っておらず、5 箇所の自オリジン解決がすべて横断層経由になっており、既存テストがグリーン
  - _Requirements: 1.1, 1.6, 3.5, 3.6, 3.7_
  - _Depends: 2.3, 2.4_

---

- [x] 3. Status レンダリンググルーを単一実装に集約する（振る舞い完全不変）

- [x] 3.1 レンダリング組み立ての単一実装と投票解決ポートを用意する
  - 解決済みの材料から Status のレンダリング入力を組み立てるコンポーネントを新設する。責務は「材料から組み立てる」ことに限り、ブースト先の解決と可視性判定は含めない
  - 経路ごとの差異を 2 つの注入点に押し出す：ミュート判定の材料（省略時はミュート無しとして扱う）と、投票の解決経路
  - 投票解決は差し替え可能なポートとして定義する。ジェネリックではなくボックス化した非同期トレイトにする（投稿エンドポイントの状態型が既に 6 個のジェネリック引数を抱えているため）
  - N 件版を主とし、1 件版は長さ 1 の呼び出しとして委譲する。この段階では内部は逐次処理のままでよい（バッチ化は次段階）
  - 完了状態：新コンポーネントの単体テストが、ミュート材料の有無で `muted` の出力が切り替わること、投票解決ポートの実装差で投票行が無い場合の挙動（エラー／省略）が切り替わることを示す
  - _Requirements: 4.2, 4.3, 4.4, 4.5, 4.6_
  - _Boundary: StatusRenderAssembler, PollResolver_

- [x] 3.2 投稿エンドポイントを単一実装に切り替える
  - 投稿エンドポイントの組み立てメソッド群を削除し、新コンポーネントへの委譲に置き換える。ブースト先の解決と可視性判定は投稿エンドポイント側に残す
  - 投票解決ポートは投票サービス経由の実装を供給する（可視性チェックを含み、行が無い／見えない場合はエラー）。5 経路のうちこれだけが解決経路そのものが異なるため最初に扱い、ポートの契約が妥当かを検証する
  - 完了状態：投稿エンドポイントに組み立てグルーが残っておらず、その既存テストと golden 契約テストがグリーン
  - _Requirements: 1.1, 1.6, 4.1, 4.3_
  - _Boundary: StatusesEndpoints_

- [x] 3.3 (P) アカウント別投稿一覧を単一実装に切り替える
  - 組み立てメソッド群を削除し新コンポーネントへ委譲する。投票解決ポートは「行必須・このモジュール固有の未検出エラー」の実装を供給する
  - 完了状態：当該モジュールに組み立てグルーが残っておらず、既存テストがグリーン
  - _Requirements: 1.1, 1.6, 4.1, 4.3_
  - _Depends: 3.2_
  - _Boundary: AccountStatusesProvider_

- [x] 3.4 (P) 通知サービスを単一実装に切り替える
  - 組み立てメソッド群を削除し新コンポーネントへ委譲する。投票解決ポートは「行必須・このモジュール固有の未検出エラー」の実装を供給する
  - 閲覧者を非省略型で受けていた箇所は、省略可能型に設定済みの値を渡す形にする。挙動は同一
  - 完了状態：当該モジュールに組み立てグルーが残っておらず、通知の既存テストと golden 契約テストがグリーン
  - _Requirements: 1.1, 1.6, 4.1, 4.3_
  - _Depends: 3.2_
  - _Boundary: NotificationService_

- [x] 3.5 (P) タイムラインハイドレータを単一実装に切り替える
  - 組み立てメソッド群を削除し新コンポーネントへ委譲する。**5 経路で唯一ミュート材料を注入する経路**であり、フィルタ文脈のミュート集合を渡す
  - 現在時刻はフィルタ文脈由来の値をそのまま渡す（他 4 経路はランタイムのクロック由来）
  - 投票解決ポートは「行任意・行が無ければ省略」の実装を供給する
  - 完了状態：当該モジュールに組み立てグルーが残っておらず、ミュート済みアカウントの投稿がタイムライン経由でのみ `muted` を真として返すことを既存またはこの段階で追加したテストが示す
  - _Requirements: 1.1, 1.6, 4.1, 4.3, 4.5_
  - _Depends: 3.2_
  - _Boundary: StatusHydrator_

- [x] 3.6 (P) 検索ハイドレータを単一実装に切り替える
  - 組み立てメソッド群を削除し新コンポーネントへ委譲する。投票解決ポートは「行任意・行が無ければ省略」の実装を供給する
  - このモジュールのドキュメントに残っている「4 番目の複製、共有ヘルパーを切り出す潮時」という CONCERN を削除する（本段階で解消されるため）
  - 完了状態：当該モジュールに組み立てグルーと該当 CONCERN が残っておらず、検索の既存テストがグリーン
  - _Requirements: 1.1, 1.6, 4.1, 4.3, 4.6_
  - _Depends: 3.2_
  - _Boundary: SearchHydrator_

- [x] 3.7 5 経路の表現一致を検証する統合テストを追加する
  - メディア・タグ・絵文字・投票・ブーストを持つ 1 つの投稿を、単体取得／アカウント別一覧／通知／タイムライン／検索の 5 経路から取得し、`muted` を除く全フィールドが一致することを検証する
  - ミュート済みアカウントの投稿について、タイムライン経由では `muted` が真、他 4 経路では偽であることを検証する
  - 完了状態：上記 2 つの検証が通り、いずれかの経路だけを変更すると落ちる状態になっている
  - _Requirements: 4.1, 4.4, 4.5_
  - _Depends: 3.3, 3.4, 3.5, 3.6_

---

- [x] 4. 一覧取得のクエリ数を件数から切り離す

- [x] 4.1 (P) 投稿とメディアの一括取得を追加する
  - 複数の投稿 id からメディア id の対応表を一度に引く関数と、複数のメディア id からメディア実体の対応表を一度に引く関数を追加する
  - 既存の単数版は削除しない（他に呼び出し元がある）。複数版は単数版と同じ絞り込み条件・同じ並び順を使う
  - 空のスライスを渡した場合はクエリを発行せず空を返す
  - 完了状態：「単数版を N 回呼んだ結果と複数版を 1 回呼んだ結果が一致する」ことを検証する単体テストが通る
  - _Requirements: 5.1, 5.4_
  - _Boundary: StatusRepository, MediaRepository_

- [x] 4.2 (P) タグの一括取得を追加する
  - 複数の投稿 id からタグの対応表を一度に引く関数を追加する。既存の単数版は残す
  - 完了状態：「単数版 N 回 == 複数版 1 回」の単体テストが通り、空スライスでクエリを発行しない
  - _Requirements: 5.1_
  - _Boundary: TagRepository_

- [x] 4.3 (P) インタラクション状態の一括取得を追加する
  - 閲覧者と複数の投稿 id から、お気に入り済み／ブックマーク済み／ピン留め済み／ブースト済みの投稿 id 集合をそれぞれ一度に引く 4 関数を追加する
  - 完了状態：4 関数それぞれについて「個別存在チェックを N 回した結果と一括取得 1 回の結果が一致する」単体テストが通る
  - _Requirements: 5.1, 5.5_
  - _Boundary: InteractionRepository_

- [x] 4.4 (P) 投票の一括取得を追加する
  - 複数の投票 id から投票実体の対応表と集計結果の対応表をそれぞれ一度に引く関数を追加する。集計は閲覧者の投票済み判定を含む
  - 完了状態：「単数版 N 回 == 複数版 1 回」の単体テストが通り、閲覧者の投票済み判定が単数版と一致する
  - _Requirements: 5.1_
  - _Boundary: PollRepository_

- [x] 4.5 組み立ての N 件経路を一括取得とメモ化に切り替える
  - N 件版の組み立てを、対象となる全投稿（ブースト先を含む）の id・著者 id・投票 id を先にまとめてから、一括取得を呼ぶ形に変える
  - 絵文字は全投稿の本文と投票選択肢のタイトルから短縮コードを集めて 1 回で解決する
  - アカウント解決は 1 回の組み立て呼び出し内で著者ごとにメモ化する。**呼び出しをまたぐキャッシュは持たない**（ステイルなデータを返す新しい失敗モードを作らないため）
  - 一括取得の結果に含まれない id は現行の各経路の規約どおり縮退させる（メディアは黙って省く、投票は解決ポートの実装が決める）
  - 出力の並び順は入力の順を維持する
  - **1 件ずつ `assemble_one` をループしている 3 つの一覧経路（アカウント別投稿一覧・検索・通知）を、
    ページ分をまとめて `assemble_many` を 1 回呼ぶ形に変える。** ブースト先の解決と可視性の再判定は
    ループの外へ持ち上げる（各経路の判定規約そのものは変えない）。これをやらないと当該 3 経路は
    件数比例のまま残り、Requirement 5.1 が未達になり、著者メモ化の項目も一度も効かない
  - 完了状態：5 経路すべての既存テストと golden 契約テストがグリーンで、一覧レスポンスの JSON と並び順がバッチ化前と一致する
  - _Requirements: 1.1, 1.6, 5.1, 5.2, 5.3, 5.4, 5.5, 5.6, 5.7_
  - _Depends: 4.1, 4.2, 4.3, 4.4_

- [x] 4.6 クエリ数が件数に依存しないことを検証するテストを追加する
  - 1 件の一覧と 20 件の一覧で、投稿固有の付随データ（メディア・タグ・絵文字・インタラクション状態・投票）の取得回数が変わらないことを検証する
  - 同一著者の投稿を複数含む一覧で、著者の解決回数が投稿数ではなく著者の種類数に一致することを検証する
  - 完了状態：上記 2 つの検証が通り、逐次処理に戻すと落ちる状態になっている
  - _Requirements: 5.1, 5.2, 5.3_
  - _Depends: 4.5_

---

- [x] 5. 複合書き込みを原子化する

- [x] 5.1 書き込みリポジトリ関数を任意のエグゼキュータで実行できるようにする
  - 投稿挿入・メディア添付・カウンタ調整・投票挿入・タグ登録・タグ関連付け・お気に入り追加/削除を、接続プールでもトランザクションでも実行できる形に変える
  - この変換は純粋に追加的であり、接続プールはそのまま渡せるため既存の呼び出し側は変更不要
  - 完了状態：対象関数がトランザクションを渡して呼べるようになっており、既存の呼び出し側を一切変更せずに全テストがグリーン
  - _Requirements: 6.3_
  - _Boundary: StatusRepository, PollRepository, TagRepository, InteractionRepository_

- [x] 5.2 投稿作成を単一トランザクションにまとめる
  - 投稿本体の挿入・メディア添付・投票挿入・タグ登録・親投稿の返信カウント加算を 1 つのトランザクションに入れ、すべて成功した場合のみコミットする
  - **Activity の配送と通知の発行は必ずコミット後**に行う。ネットワーク I/O をトランザクション内に入れると接続を占有し、配送失敗が正常なローカル書き込みを巻き戻す
  - べき等キーの束縛処理の実際の呼び出し位置を確認し、トランザクションに含めるべきかを判断して記録する
  - 完了状態：途中で失敗させたとき、投稿行・メディア関連・投票・タグ・親投稿の返信カウントのいずれも変化していないことを統合テストが示す。成功時のレスポンスとカウンタ値は従来と一致する
  - _Requirements: 1.2, 1.6, 6.1, 6.3, 6.4, 6.5_
  - _Depends: 5.1_
  - _Boundary: StatusService_

- [x] 5.3 ブースト・お気に入り・お気に入り解除を単一トランザクションにまとめる
  - レコードの追加/削除とカウンタの更新を 1 つのトランザクションに入れる。追加/削除が実際に発生したかの戻り値による分岐はトランザクション内に残す
  - Activity の配送と通知の発行はコミット後に行う
  - 完了状態：途中で失敗させたとき、レコードとカウンタのいずれか一方だけが変化した状態が残らないことを統合テストが示す。成功時の観測結果は従来と一致する
  - _Requirements: 1.2, 1.6, 6.2, 6.3, 6.4, 6.5_
  - _Depends: 5.1_
  - _Boundary: InteractionService_

- [x] 5.4 原子性の検証テストを追加する
  - 投稿作成とお気に入りの少なくとも 2 つについて、複合書き込みの途中で失敗させ、カウンタと実体レコードの両方が変化していないことを検証する
  - 完了状態：上記の検証が通り、トランザクションを外すと落ちる状態になっている
  - _Requirements: 6.1, 6.2, 6.3, 6.4_
  - _Depends: 5.2, 5.3_

---

- [x] 6. モジュール配線シーケンスを単一実装にする

- [x] 6.1 配線シーケンスを単一の合成関数に抽出する
  - 9 モジュールの構築順（特に「投稿モジュール構築 → アカウントポート登録 → ソーシャルグラフモジュール構築」の順序制約）を持つ関数を新設する
  - 入力は接続プール・ランタイム・アクターモジュール・設定・連合 HTTP クライアント。設定の読み込み・接続プール作成・マイグレーション・アクター配線・リスナー bind・シャットダウン信号の扱いは呼び出し元に残す
  - バックグラウンドタスクのハンドルは spawn せずに返す（シャットダウン信号が経路ごとに異なるため）
  - 連合 HTTP クライアントは 1 インスタンスを共有する形にする。連合ペアハーネスが既に 1 インスタンス共有で動作しており実証済みで、送出されるリクエスト自体は不変
  - trait 抽象を被せない。起動時に 1 回だけ選ぶ配線であり、差分は具体値に過ぎない
  - 完了状態：合成関数が単独でコンパイルでき、11 段階の順序が 1 箇所に存在している
  - _Requirements: 7.1, 7.2, 7.3, 7.5_
  - _Boundary: compose_modules_

- [x] 6.2 本番起動経路を合成関数に移行する
  - 本番の状態構築から配線部分を削除し、合成関数の呼び出しに置き換える。設定読み込み・接続プール・マイグレーション・アクター配線・実リスナーでの待ち受けとシャットダウン処理は残す
  - バックグラウンドタスクは本番の OS シャットダウン信号で spawn する
  - 完了状態：本番経路に配線シーケンスの複製が残っておらず、起動ライフサイクルの既存テストがグリーン
  - _Requirements: 1.6, 7.1, 7.5, 7.6_
  - _Depends: 6.1_

- [x] 6.3 テストハーネスを合成関数に移行する
  - 通常のテストアプリ起動から配線部分を削除し、合成関数の呼び出しに置き換える。決定的ランタイム・スキーマ隔離プール・エフェメラルなリスナー bind は残す
  - バックグラウンドタスクは従来どおり終わらない信号で spawn する
  - 完了状態：テストハーネスに配線シーケンスの複製が残っておらず、テストハーネスを使う既存テストがグリーン
  - _Requirements: 1.6, 7.1, 7.5_
  - _Depends: 6.1_

- [x] 6.4 連合ペアハーネスを合成関数に移行し、既存の配線欠落を是正する
  - 連合ペア起動から配線部分を削除し、合成関数の呼び出しに置き換える。呼び出し元供給のループバック用 HTTP クライアントを渡す点は残す
  - **この移行により、連合ペアハーネスで欠落していたアカウントポート登録が自動的に補われる。** その結果、ペアの各インスタンスのアカウント表現は組み込み既定ではなく本番と同じ実装を通るようになる。
    **[task 6.4 実装・レビューで訂正] このタスク文と design.md の記述「投稿数 0・投稿一覧が空」は不正確だった。**
    実際に空だったのはアカウントの**投稿一覧**のみ。`statuses_count` は当初の想定と異なり
    `social_graph::build_social_graph_module`（このハーネスが元々実行していた段階）が
    `AccountCountsContribution::new(pool)` を自前で構築して上書きするため、
    `register_account_ports` の欠落があっても影響を受けていなかった（`social_graph.rs:614-622` と
    `statuses.rs:627-648` で確認、RED フェーズでも実測: 投稿一覧のみ `Some(0)` → `Some(1)` で
    失敗し `statuses_count` は当初から一致していた）。**task 6.5 の前提「登録が呼ばれていなければ
    0 になり」は `statuses_count` には当てはまらない。** 6.5 の順序入れ替え検知は
    followers/following カウント（`social_graph` 側の段階順）で行うこと。
  - 期待値が変わった既存の連合テストについて、それが「これまで誤った前提でグリーンだった」ことの是正である根拠を実装記録に残す
  - 完了状態：連合ペアハーネスに配線シーケンスの複製が残っておらず、連合テストがグリーンで、期待値を変更したテストの一覧と各々の正当性が記録されている
  - _Requirements: 1.4, 1.6, 7.1, 7.5, 7.7_
  - _Depends: 6.1_

- [x] 6.5 配線順序の検証テストを追加する
  - フォロワーと投稿を持つアクターについて、アカウント表現の各カウントが実データを反映した非ゼロの値であることを検証する。
    **[task 6.5 実装・レビューで訂正] 「アカウントポート登録が呼ばれていなければ 0 になり」は不正確だった。**
    実測では stage 7 (`register_account_ports`) と stage 8 (`build_social_graph_module`) の順序を
    入れ替えると `followers_count`/`following_count` のみが 0 になり、`statuses_count` は影響を
    受けない（`social_graph::build_social_graph_module` の `CombinedAccountCountsProvider` が
    `AccountCountsContribution` を自前構築し stage 7 の登録を読み戻さないため）。
    stage 7 を丸ごと落とした場合は逆に 3 カウントは全て正しいまま、投稿一覧のみが空になる。
    2 つの故障モードは独立しており、テストは両方を別ケースで検証する。
  - アカウントの投稿一覧が空でないこと（組み込み既定の空プロバイダが使われていないこと）を検証する
  - 完了状態：上記 2 つの検証が通り、合成関数内で登録順を入れ替えると落ちる状態になっている
  - _Requirements: 7.3, 7.4_
  - _Depends: 6.2, 6.3, 6.4_

---

- [x] 7. 全体の回帰確認
  - 全段階完了後にフルテストスイートを実行し、グリーンであることを確認する
  - 既定設定の静的解析で警告がゼロであることを確認する
  - 全段階を通じて変更した既存テストの期待値と、各々が仕様変更ではなく「テスト側が誤っていた」ことの修正である根拠を一覧にまとめる
  - 完了状態：フルスイートがグリーン、既定の静的解析が警告ゼロ、テスト期待値の変更一覧とその根拠が揃っている
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6_

---

## Implementation Notes

- **テストスイートは HEAD 時点でグリーンではない。** `9bc8c26`（本 spec 着手前）で
  `cargo test --lib -- notifications::` を実行すると 15 件が失敗する：14 件は
  `notifications::service::tests` のフィクスチャがハイフンを含むハンドル
  （`"clear-recipient"` 等）を使い、`Handle::new`（`src/actor/model.rs:60`）が
  ASCII 英数字とアンダースコアのみを許すため。1 件は
  `notifications::endpoints::tests::list_notifications_returns_recipient_notifications_newest_first`
  の並び順アサーション。**本 spec のスコープ外の既存不具合**であり、
  Requirement 1.6 の「グリーン」は「HEAD の既存失敗集合を増やさない」と読み替える。
  各段階でこの 15 件以外の失敗が出たらそれは本 spec による回帰。
- **フル並列でのテスト実行は接続枯渇で偽の失敗を大量に出す。** PostgreSQL の
  `max_connections` が 100 に対し、`spawn_test_app` 1 回あたりプール 5 + admin 1 の
  最大 6 接続を使い、連合ペアテストはその 2 倍。16 論理 CPU の既定並列度では
  200 件超が `PoolTimedOut` で落ちる（コードの問題ではない）。
  **`cargo test --lib -- --test-threads=6` を使うこと。**
- **テスト実行プロセスを強制終了しない。** 中断すると `kawasemi_test` に
  `kawasemi_test_harness_<nanos>_<seq>` スキーマが残り続ける（既知課題 B-5、本 spec の
  スコープ外）。1 回の中断で 432 件を検出した。掃除は
  `DROP SCHEMA IF EXISTS ... CASCADE` を prefix 一致で流す。
- **`AppError` へのフィールド追加は呼び出し側を壊さない。** 構造体リテラルによる直接構築は
  `src/error.rs` 内のコンストラクタ 2 箇所のみで、他 70 件のヒットはすべて
  `-> AppError {` という関数シグネチャだった。
- **`map_*_error` 26 件の内訳が確定した**（design.md の Open Question の答え）：23 件が
  `AppError::server(INTERNAL_SERVER_ERROR, source)` でバイト単位に同一。残る 3 件
  （`actor/repository.rs` / `federation/outbound/queue.rs` / `statuses/status_repository.rs` の
  `map_insert_error`）は unique 制約名を見て 4xx に振り分けるため集約しない。
- **絵文字解決の食い違いは 2 軸あった（解消済み）。** 分析レポートは `muted` だけを
  差異として挙げていたが、実測すると絵文字も食い違っており、しかも「投稿本文」と
  「投票選択肢」で独立していた（search は本文のみ解決）。ユーザー判断を仰いだ結果
  「リファクタと一緒に直してよい、ただしコミットは分ける」との回答を得たため、
  `d4717aa` で全経路解決に統一。`vote_poll` の同種の食い違いも `282b336` で解消。
  いずれも既存テスト・golden への影響はゼロだった。
- **バグを先に直したほうが総作業量は小さい**（この判断の根拠）。残していると
  `EmojiResolution` を task 4 のバッチ化まで引きずり、条件分岐付きで一度書いて後で
  外す二度手間になる。また task 3.7 の一致テストが 5 経路中 2〜3 経路で該当フィールドを
  除外する骨抜きの比較になり、再乖離を防ぐ役に立たなくなる。
- **task 3.7 が「リクエスト起点かどうか」による正当な差異を炙り出した。** リクエストを
  持つ経路は `Host`/`X-Forwarded-Proto` から、持たない経路は設定ドメイン + https から
  URL を組み立てる。どちらも正しいので、テスト側が実クライアント相当のヘッダを送る形で
  合わせた。バグではない。
- **一括取得関数の戻り値は「該当行のない id はキーごと不在」。** `media_ids_for_statuses` は
  「添付ゼロの既存 status」と「存在しない status」を区別せずどちらもキー不在に縮退する。
  task 4.5 の「一括取得の結果に含まれない id は縮退させる（メディアは黙って省く）」と一致する契約。
  規約は design.md ではなく各関数の doc comment に固定してあるので、呼び出し側を書くときはそちらを読む。
- **一括取得の戻り値は関数ごとに粒度が違う。** `media_ids_for_statuses` は id だけを返し、
  `tags_for_statuses` は `Tag` 実体（id/name/created_at）を返す。どちらも design.md の
  Service Interface どおりなので揃えないこと。task 4.5 の呼び出し側で取り違えやすい。
- **`PollResolver` の 5 実装は投票 id をループして単数版を呼んでいる**（4.5-A のレビューで判明）。
  そのため task 4.4 で追加した `find_polls_by_ids` / `tally_many` には本番の呼び出し元が
  1 つも無く、**投票だけが件数比例のまま残っている**。Requirement 5.1 は投票を明示的に
  列挙しているので、配線するまで 5.1 は未達。→ **4.5-B で 4 実装を配線して解消。**
  `statuses/endpoints.rs:442` の `PollServiceResolver` だけは据え置いた。design.md の
  `#### PollResolver` Implementation Notes が「この経路は単一 Status の取得が主用途なので
  順に `poll_service.poll()` に渡す実装で構わない（Requirement 5.1 が対象とするのは一覧経路）」と
  明文で許容しており、一括化には `PollService` に可視性チェック付きの複数版が要るため。
- **クエリ数の一次情報は task 4.6 のテストであって、この散文ではない。** 計測機構は
  `src/test_harness/query_log.rs`（`#[cfg(test)]`）で、sqlx の `sqlx::query` tracing イベントを
  フューチャ単位の Layer で捕捉して SQL 全文で分類する。**tracing のコールサイト interest
  キャッシュ汚染で約 1/5 の確率で捕捉ゼロになる罠がある**（非計測スレッドが先に登録すると
  `never` が焼き付く）。スコープ内での `rebuild_interest_cache()` + ウォームアップ文の有界
  リトライ + **ウォームアップ捕捉の自己 assert** で解消済み。この自己 assert を外すと
  「クエリ 0 本」が偽の成功に化けるので、絶対に外さないこと。
- **`account_provider::visible_to` は候補ごとに関係解決を発行する**（4.6 で新たに判明、
  `account_provider.rs:406` のループが `viewer_relation` を呼ぶ）。Requirement 5.1 の列挙外で
  design.md も可視性判定は呼び出し元に残すと明記しているが、Requirement 5 の Objective からは
  未達。4.6 のテストが候補数と等値で pin してあるので黙って回帰することはない。
- **`endpoints.rs` の 3 経路では投票だけが件数比例のまま残る**（4.5-C3、ユーザー判断）。
  `PollServiceResolver` は `PollService::poll` 経由で投票ごとに `visible_poll_and_status` を
  かけるため据え置いた。一括化には `PollService` に可視性チェック付きの複数版が要る。
  **design.md の `#### PollResolver` が据え置きを認めた根拠「この経路は単一 Status の取得が
  主用途」は、context とブックマーク一覧が同じ surface にある以上、読める範囲より狭い。**
  Requirement 5.1 は投票を明示列挙しているので、これは満たされた基準ではなく実在の残余。
  モジュール doc と `PollServiceResolver` の doc に記載済み（design.md は編集していない）。
- **`assemble_one` の本番呼び出し元は `endpoints.rs:525` の 1 箇所だけになった**（4.5-C3）。
  `render_status_json` を `render_status_page` へ委譲させると本番呼び出し元がゼロになり
  `dead_code` で `-D warnings` が落ちる。design.md:269 が `assemble_one` を「長さ 1 の
  薄いラッパー」と明示規定しているので設計準拠ではあるが、残すかは `render_assembler.rs` を
  所有する立場で別途決めること。
- **通知経路には件数比例が残る（受入基準は充足、Objective は未達）**（4.5-C2 のレビューで確定）。
  エンベロープの `account_json`（= `show_account`）が通知ごとに N 回、関連投稿と
  ブースト先の `find_by_id` が最大 2N 回。Requirement 5.1 が列挙するのはメディア・タグ・
  絵文字・インタラクション状態・投票で、5.2/5.3 の「著者」は design.md のトレーサビリティ上
  `StatusRenderAssembler` の著者メモ化に割り当てられている。**通知の origin は Status の
  著者ではない**ので受入基準からは外れるが、Requirement 5 の Objective からは未達。
  一括化には `AccountService` の複数 id 版が要る（現状は入口が無い）。
- **`account_provider` のフィルタ鎖に Requirement 5.1 の未達分が残っている**（4.5-C1 のレビューで判明）。
  `passes_filters` は `only_media` 指定時に `media_ids_for_status`、`pinned` 指定時に
  `exists_pin` を**候補ごとに**発行する。どちらも 5.1 が列挙するメディア／インタラクション状態。
  `media_ids_for_statuses` と `pinned_status_ids` で一括化でき、**どの status がページに届くかは
  変わらない**（フィルタ結果は同一）。4.5-C1 は「フィルタ鎖を変更しない」を境界にしたため未着手。
  task 4.6 の検証テストがこれを拾えるようにすること。
- **一覧形状のループは 3 経路ではなく 5 経路あった**（4.5-B のレビューで確定）。既知の
  `account_provider.rs:361` / `search/hydrator.rs:316` / `notifications/service.rs:351` に加えて、
  **`statuses/endpoints.rs:810` / `:814`（context の ancestors・descendants）と `:1103`
  （ブックマーク一覧）**も 1 件ずつ `render_status_json` を呼んでいる。PART C の対象。
- **`find_polls_by_ids` → `tally_many` の間で poll が消えるレース窓の縮退が変わった**（4.5-B）。
  旧実装は `tally` 側の 404 が伝播したが、新実装は「poll は在ったが tally が無い」を
  「最初から無かった」と同一視する。`account_provider` だけ文言が `"poll not found"` →
  `"status not found"` になる。並行 DELETE 下では旧実装も削除タイミング次第で両方の応答を
  返し得たので、**変更前に不可能だった応答を新たに導入してはいない**（Requirement 1.1 は
  決定的な写像を前提にしている）。ステータスコードと `ErrorKind` は全ケースで同一。
- **`assemble_many` のクエリ数は件数 M に依存しない**（K は著者の異なり数）。内訳は
  media_ids 1 + find_by_ids 1 + tags 1 + interaction 4（viewer 無しなら 0）+ resolve_emojis 1
  + `resolve_many`（4.5-B 以降は内部で `find_polls_by_ids` 1 + `tally_many` 4、viewer 無しなら 3）
  + `show_account` K 回。**正確な実数を散文で追うのはやめること**（4.5-B で 2 回ずれた）。
  task 4.6 の検証テストが一次情報になる。`render_input` を同期関数にしてあるため、描画ループに
  リポジトリ呼び出しを入れることが構造的に不可能になっている（この性質を壊さないこと）。
- **`tests/notification_contract_it.rs` は HEAD 時点で 9 件失敗する（本 spec のスコープ外）。**
  テストは `tests/golden/notifications/notification_contract_it_*.json` を参照するが、
  実ファイルは `notification_*.json`。notifications spec の着地時点（`d212228`）からの
  ファイル名不一致。**task 4.5 の完了条件「golden 契約テストがグリーン」は通知については
  満たせない。** task 7 の既存不具合リストに載せること。
- **`assemble_many` を呼んでいたのはタイムラインだけだった**（2026-08-04 に判明）。
  アカウント別投稿一覧 (`account_provider.rs:361`)・検索 (`search/hydrator.rs:316`)・
  通知 (`notifications/service.rs:351`) は 1 件ずつ `assemble_one` をループしていた。
  アセンブラ内部だけをバッチ化しても当該 3 経路は件数比例のまま残るため、**ユーザー判断により
  task 4.5 のスコープを広げ、3 経路も `assemble_many` 呼び出しへ変換する**ことにした。
  実装は 2 段階に分ける（アセンブラ内部 → 呼び出し側）。順序が逆だと検証できないため。
- **design.md の Open Question「`tally` の SQL を素直に複数化できるか」の答えは「できる」。**
  単数版 `tally` は 1 本の複合クエリではなく独立した 4 本（存在確認 / `poll_options` を
  `ORDER BY idx` / `COUNT(DISTINCT actor_id)` / viewer の `choice`）で、相関サブクエリも
  JOIN も無い。各本を `= ANY` に広げ `ORDER BY` の先頭に `poll_id` を足すだけで済んだ。
  **`votes_count` と `voters_count` は別クエリのまま保つこと**（1 本の JOIN にまとめると
  option 行 × vote 行で双方が膨張する）。クエリ数は viewer が `Some` で 4・`None` で 3 の定数。
  `tally_many` の結果マップのキーは `polls` 行から作る（option 行由来にすると
  「存在するが選択肢ゼロの poll」がキーごと消え、空 `options` を返す単数版と食い違う）。
- **`reblogged_status_ids` が返すのは「ブースト元 status の id」であってブースト行自身の id ではない。**
  単数版 `find_reblog` はブースト行（`Status`）を返すので、単数版と複数版で値の空間が違う。
  複数版は「入力に渡した id のうち viewer がブーストしたもの」の部分集合を返す。
  `statuses` に `(actor_id, reblog_of_id)` の一意制約は無いが `HashSet` が重複を吸うため
  `LIMIT 1` の対応物は置いていない。task 4.5 の呼び出し側で取り違えやすい。
- **一括取得の `find_by_ids` は所有者スコープを持たない**（単数版 `find_by_id` と同じ `WHERE` にする
  design.md の制約に従った結果）。添付ハイドレーションには正しいが、**所有者スコープが要る経路
  （メディア編集系）は引き続き `find_owned` を使うこと。** task 4.5 / 5.1 で踏みやすい。
- **`PoolTimedOut` は HEAD 時点から発生する既存問題。** ベースライン (9bc8c26) で
  フルスイートを並列度 4 で実行すると 245 件失敗し、うち 230 件が `PoolTimedOut`。
  本ブランチ (task 1〜3 適用後) は 163 件失敗・うち 148 件が `PoolTimedOut`。
  実質的な失敗は両者とも同一の 15 件。したがって回帰判定は「15 件以外が出ないこと」で行う。
  孤立スキーマが 1 回のフル実行で 600 件超蓄積し、カタログ肥大が接続取得を遅らせて
  タイムアウトを誘発する（既知課題 B-5、本 spec のスコープ外）。**検証前に必ず
  孤立スキーマを掃除すること。**
- **task 5.1 完了時点で `--test-threads=6` のベースラインが崩れている。** レビュー時点で
  同コマンドは 261〜265 failed（うち約 256 が `PoolTimedOut`）と非決定的。**変更前の
  `main` に対して同一コマンドを実行しても 229 failed / 217 PoolTimedOut** となり、これは
  本 spec と無関係な環境劣化（コネクション枯渇の悪化、既知課題 B-5 の延長）。
  **task 5.2 以降の検証は `--test-threads=2` を使うこと。** 15 件ベースラインは
  現状 threads=2 でのみ意味のある形で成立する。
- **task 5.1: 書き込み関数は 2 つの sqlx バウンドを使い分ける。** 単文の書き込み
  （`insert_status` / `adjust_counts` / `upsert_tag` / `associate_tag` / `add_favourite` /
  `remove_favourite`）は `PgExecutor<'e>`（`social_graph/repository.rs` の task 2.2 先例と同じ
  慣習）。複数文の書き込み（`attach_media` のループ、`insert_poll` の内部 begin/commit）は
  `Acquire<'a, Database = Postgres>`。`insert_poll` は自前の begin/commit を保持したまま
  `Acquire::begin` 経由にしたため、外側トランザクション配下では **nested SAVEPOINT** になる
  （プールから直接呼べば従来どおり独立トランザクション）。task 5.2 で `insert_poll` を
  外側 tx に含める際、この SAVEPOINT の 2 往復コストは許容範囲だが気に留めること。
  既存呼び出し側は 1 箇所も変更していない（純粋追加）。
- **task 5.3: `unreblog` は原子化の対象外のまま残っている。** タスク文・Requirement 6.2・
  design.md トレーサビリティ行 6.2 のいずれも「`InteractionService` の 3 メソッド」
  （reblog/favourite/unfavourite）に限定しており仕様どおりだが、`status_repository::delete_status`
  （ブースト解除のレコード半分）が `&PgPool` 取りの自前トランザクションのままで、
  5.3 の境界内では原子化できない。**feature レベル検証（task 7 / `/kiro-validate-impl`）への
  申し送り事項。** 是正するなら `delete_status` のエグゼキュータ汎用化が前提になる。
  `find_reblog` は 5.3 の実装過程で `PgExecutor<'e>` に変換済み（5.1 の境界の範囲内、純追加）。
- **task 5.2: `attach_media` はトランザクション reborrow から直接呼べない。** `attach_media<'a, A: Acquire<'a>>`
  （5.1 で追加）に `A = &mut *tx` を渡すと、`Acquire::Connection` 関連型を await をまたいで保持する形が
  reborrow のライフタイムに対して汎用化されすぎ、rustc が `implementation of sqlx::Acquire is not
  general enough` を出して axum ハンドラの `Send` 境界（`server.rs:674`）が壊れる（レビューで再現確認済み）。
  対応として `status_repository::attach_media_on_conn(conn: &mut PgConnection, ..)` を追加し、
  `attach_media` はそれに委譲する形にした（ループ本体は完全に同一、SQL 変更なし、既存 8 呼び出し側は無変更）。
  `insert_poll` は具体的な `sqlx::Transaction` を保持するため同じ問題を踏まない。
  **task 5.3（InteractionService）はこの問題を踏まない**（そちらの書き込みはすべて `PgExecutor`）。
- **task 5.2: べき等キーの束縛は意図的にトランザクション外・配送後の元の位置に残した。**
  `status_idempotency_keys.status_id` は `statuses(id)` への `NOT NULL` FK
  （`migrations/0007_statuses.sql:243`）なので、コミット前に束縛するとロールバック時にダングリング
  参照を残すリスクがある。`idempotency::bind` は `&PgPool` を取る（5.1 が意図的に変換対象から外した
  モジュール）。残存する未解決の窓：commit 成功後 bind 失敗時、リトライで投稿が重複作成される。
  これは本タスク着手前から存在する既知の残余で、幅は変わっていない（Requirement 6.1 の列挙は
  投稿本体・メディア添付・投票・タグ・親投稿の返信カウントのみで、べき等キー自体は対象外）。
- **task 6.1: `ModuleWiringInput` に design.md 未記載の 6 つ目のフィールドを追加した
  （`federation_cadence: FederationPollCadence`）。** 本番と 2 つのテストハーネスで連合
  バックグラウンドループのポーリング間隔が実際に異なる（本番 5s/20/1h、`test_harness.rs` 200ms/20/5s、
  `federation/test_harness.rs` 100ms/20/5s）ため、design.md 固定の 5 フィールドでは表現できない。
  レビューで実値を確認済み・design.md への差し戻しは不要と判断（`domain`/`secure_mode`/TTL/retention の
  `AppConfig` 由来導出は関数内に 1 箇所のまま）。**task 6.2/6.3/6.4 で cadence の値を移すとき、
  この struct を見落とさないこと。**
  `ActorDirectory` の共有化（`federation/test_harness.rs` は既に共有、他 2 経路は個別構築だった）も
  同時に統一した。`ActorDirectory` は `PgPool` のみを保持し内部状態を持たないため挙動同一（レビューで実測確認）。
- **task 6.1: `compose_modules` はまだどこからも呼ばれていない（未配線）。** `src/bootstrap/wiring.rs`
  として単独コンパイルするのみ。`dead_code` の `#[allow]` は `bootstrap.rs` の `mod wiring;` 宣言
  1 行にのみ範囲を絞ってある（task 6.2 で最初の呼び出し元ができたら削除する）。
- **task 5.4: ロールバック側は 5.2/5.3 で追加した 3 テストが既にカバー済みだった。** 実装者・レビュアー
  双方が独立にトランザクションを外して（`pool.begin()`→`pool.acquire()`+`drop(tx)`）3 テストが
  実測で落ちることを確認済み。**Requirement 6.3（成功パスでカウンタと実体レコード数が一致）は
  未検証のまま残っていた実在のギャップ**で、既存の成功系テストはカウンタしか見ておらず実体行数を
  数えていなかった。2 本の新規テスト（`favourite_counter_matches_the_actual_favourite_row_count_at_every_step` /
  `reply_counter_matches_the_actual_reply_row_count_after_successful_creates`）で埋めた。
  変異テスト（カウンタ加算量を書き換える等）で非空虚性も実測確認済み。
- **フルスイート実行はこの環境で複数エージェントが同時に DB を使うと信頼できない。**
  `--test-threads=2` でも同時実行が重なると `PoolTimedOut` が数百件単位で出る（コードの問題ではない）。
  対処：モジュール単位（`statuses::` 等）で実行し、フルスイートは全並行作業が止まっている時にのみ判定材料にする。
- **task 6.2: 本番起動経路の配線を `compose_modules` に置き換え、`bootstrap.rs` から重複を除去した。**
  挙動同一性の 3 点（HTTP クライアント共有・`ActorDirectory` 共有・cadence）はレビューで実測確認済み
  （[[kawasemi-structural-refactor-progress]] の task 6.1 の記録と同じ根拠）。background task の
  spawn 順序が「11 段階完了後にまとめて」に変わったが、リスナー bind より前である点は変わらず
  観測不能。`compose_modules` の `Result<_, AppError>` は既存の `BootstrapError::KeySupply` に
  素通しされる（誤った variant 名だが、配線段階自体は現状失敗し得ないため到達不能。将来配線が
  失敗し得るようになったら `BootstrapError::Wiring` を追加すること）。
  `pub(crate) mod wiring;` の `#[allow(dead_code)]` はこのタスクで削除。
- **task 6.4: 既存の連合ペアテスト 13 本は期待値変更ゼロだった。** `federation_symmetry_it.rs` /
  `statuses_federation_pair_it.rs` / `federation_pair_it.rs` の既存アサーションは DB 行や
  Relationship JSON を見ており、アカウント表現（`statuses_count` / 投稿一覧）を一切参照していない
  ため、Requirement 1.4 は「変更ゼロ」で充足。新規テスト
  `federation_pair_instances_wire_statuses_account_ports_like_production` を追加し、
  両インスタンスで投稿一覧が実データを反映することを検証（RED フェーズで実際に失敗することを
  実装・レビュー双方が独立に再現済み）。
- **task 6.3: `--lib` の既知ベースライン 15 件に加え、`--lib` の外側で 3 本の既存不具合バイナリを発見。**
  いずれも `main` 時点（本タスク着手前）で再現し、task 6.3 とは無関係：
  - `notification_generation_it`：`delivery_sink_is_called_exactly_once_per_created_notification_and_never_for_a_suppressed_event`
    が "signer is blocked by this recipient" で失敗（notification_generation_it.rs:342）
  - `notification_list_it`：`list_returns_only_the_authenticated_actors_own_notifications_newest_first`
    が scope 不足エラーで失敗（notification_list_it.rs:347）
  - `notification_contract_it`：9 件失敗。ゴールデンファイル名の不一致（ハーネスは
    `notification_contract_it_<kind>.json` を解決するが実ファイルは `notification_<kind>.json`）。
    これは 4.5 の Implementation Notes に既出の既知課題と同一。
  **task 7 の既存不具合一覧に上記 3 バイナリを追加すること。**
- **task 6.3: `compose_modules` 内で `social_graph_pending_delivery.resolve()` が
  `federation_background.spawn()` より前になった**（旧ハーネスは spawn の方が先）。
  未解決セルを配送ワーカーが観測する窓が消える方向の変化で、安全側。是正不要。
- **task 6.5: 2 つの故障モードは独立に pin してある。** `account_counts_reflect_real_data_when_the_wiring_order_is_intact`
  （stage 7/8 の順序破壊を検出、followers/following が 0 に）と
  `account_statuses_page_is_not_the_built_in_empty_default`（stage 7 の欠落を検出、投稿一覧が空に）。
  レビューが両方の変異を独立に再現して確認済み。stage 6→7 の入れ替えは
  `statuses_module.relationship_query_registry()` の型依存によりコンパイルエラーになるため
  実行時テストの対象外（型で守られている）。
  期待値 `statuses_count=2 / followers_count=1 / following_count=1` は followers と following が
  同値のため、この 2 フィールドの取り違えは検出できない（非ブロッキング、レビュー指摘）。
- **task 7: 全体の回帰確認完了。Requirement 1.4「既存テスト期待値の変更一覧」の結論は「変更 0 件」。**
  機械的根拠（`9bc8c26`＝spec 着手前 `..HEAD` 全域）：全テストファイル（`src/**/tests.rs` +
  `tests/*`）の削除行はわずか 8 行で、すべて `use` 宣言。diff 全体でも `assert|expect_|expected|panic!`
  に一致する削除行は 7 行のみで、すべてコメント散文中の "unexpected" という英単語。golden 契約ファイル
  （`tests/golden/**`）の変更は 0 件。テストファイルの削除・リネームも 0 件。
  「期待値変更に見えるが違う」もの（レビューで個別確認済み）：
  - task 1.2：重複投票判定を文字列比較から `ErrorTag::DuplicateVote` へ。メッセージ文言
    `"actor has already voted in this poll"` 自体は一字も変えていない（クライアント観測は不変）
  - task 3.7：リクエスト起点による URL 差異はバグではなく正当な差異（`api/origin.rs::self_origin`）。
    新規テストが実クライアント相当のヘッダを送る形で対応
  - task 4.5-B：`account_provider` の 404 文言が "poll not found" → "status not found" に変わったのは
    **新設**テスト（`account_provider/tests.rs:134`）内の話で、既存テストの期待値変更ではない
  - task 6.4：design.md が予見していた連合ペアテストの期待値変更（Requirement 7.7）は
    **実際には発生しなかった**（既存 13 本は無変更、新規テスト 1 本を追加して是正を検証）
  Requirement 1.3（実装側を修正しテストの期待値は変えない）は全段階で守られている。
- **task 7: フルスイート一括実行はこの環境では判定に使えない。** `--test-threads=6` の一括実行は
  PostgreSQL 接続枯渇（`max_connections=100`）で `.expect()` レベルの偽失敗を大量に出す
  （アサーション失敗ではなく `test_harness.rs` の admin 接続確立・プール確立・鍵キャッシュ読込の失敗）。
  **モジュール単位に分割し、孤立スキーマ掃除＋接続排出を挟んで実行する**（`--test-threads=2`）ことで
  判定可能になる。レビューが `9bc8c26`（spec 着手前）に対する別ワークツリーでの再実行により、
  残存する失敗集合（`--lib` 15 件 + 統合テスト 3 バイナリ 11 件）が完全に既存不具合であることを
  独立に証明済み（推測ではなく実証）。
- **task 7: 未解決として残った既知の指摘（GO 判断には影響しないが記録）**
  1. **task 3.7 のテストはタスク文が求める 5 経路ではなく 2 経路
     （`GET /statuses/:id` と `GET /accounts/:id/statuses`）の直接比較にとどまる。** `muted` の
     切り替えは既存の serializer 単体テスト（`serializer/tests.rs:124/136`）のみが担保しており、
     5 経路横断の `muted` テストが無い。5 経路が全て `StatusRenderAssembler` へ委譲していることは
     構造的に確認済みだが、比較ではなく構造でしか保証されていない。**ユーザー判断が必要**（再オープンして
     テストを追加するか、構造的保証で十分とみなすか）。
  2. `notification_contract_it`（9 件）/`notification_generation_it`（1 件）/
     `notification_list_it`（1 件）は本 spec のスコープ外の既存不具合と確定。修正は別 spec で。
- **task 3.7 のカバレッジについてユーザー判断：現状（2経路の直接比較＋構造的保証）を受け入れる。**
  5経路すべてが単一の `StatusRenderAssembler` へ委譲していることはコード構造で確認済み。
  5経路横断の契約テストは追加しない。
