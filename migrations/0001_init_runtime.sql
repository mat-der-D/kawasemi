-- 0001_init_runtime: core-runtime のマイグレーション機構を成立させる初期マイグレーション。
--
-- core-runtime 自身は永続化対象のドメインテーブルを所有しない
-- （適用履歴・チェックサムは sqlx が `_sqlx_migrations` として自動管理するため、
-- ここで手動作成してはならない）。
--
-- 本ファイルは「migrations/ ディレクトリが sqlx の埋め込みマイグレーション対象として
-- 認識される」ことと、「連番命名・前方追加のみ」という追加規約の起点を確立するための
-- no-op に近い初期マイグレーションであり、後続 spec はこのディレクトリに
-- 0002_ 以降の連番ファイルを追加していく。
